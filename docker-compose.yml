services:
  mamma-photo-viewer:
    build: .
    ports:
      - "8420:8420"
    volumes:
      - /home/joakim/Pictures/mammas_bilder:/photos:ro
      - thumbcache:/thumbcache
      - analytics_data:/data
      - zipcache:/zipcache
      - stories:/stories
    restart: "no"

volumes:
  thumbcache:
  analytics_data:
  zipcache:
  stories:
