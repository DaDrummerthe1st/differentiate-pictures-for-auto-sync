# Did not proactively self-report the Sources lapse

See [README.md](../README.md) for what belongs here.

## What happened

After missing `WebSearch`'s mandatory Sources requirement (see the adjacent, already-fixed bug filed minutes earlier), the omission was only corrected because Joakim asked directly for the sources. He then had to ask a *second* time, explicitly, whether the lapse itself had been written up as a claude-bugs entry — it hadn't been, until asked. `CLAUDE.md`'s wrap-up rule already says "once a session shows drift... say so plainly," and the "surface findings immediately" rule already covers process lapses, not just design findings — neither prompted self-filing here without being asked.

## Why it happened

Treated "fix the immediate problem" (supply the missing sources) as the complete response, without the separate step of asking "does this also warrant its own durable record per this repo's own convention" — the same gap in reasoning as the original lapse: satisfying the immediate, visible requirement while missing an adjacent standing rule that wasn't in the foreground of the exchange at that moment.

## What changed

Filed and closed this entry alongside the original. No CLAUDE.md rule change — the existing "surface findings immediately" and self-review rules already covered this — logged so a future session applies those rules to its *own* process lapses, not only to design/doc findings about the project itself, without waiting to be asked twice.
