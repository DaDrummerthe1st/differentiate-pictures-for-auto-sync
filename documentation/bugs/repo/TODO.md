# bugs/repo/TODO.md

No index is kept here — it proved unreliable (drifted out of sync repeatedly). Browse [under_process/](under_process/) directly: filenames are `<date>-<short-slug>.md`, self-describing, and each file opens with a `Status:` line conveying where it stands. `ls documentation/bugs/repo/under_process/` is the reliable way to see what's open — cheaper and more accurate than a hand-maintained list.
