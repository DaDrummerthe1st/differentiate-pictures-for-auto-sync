# rebuilt CHANGELOG.md as one-file-per-entry documentation/changelog/

Built the redesign decided earlier today (see `CHANGELOG_ARCHIVE.md`'s 2026-07-23T16:24:00+00:00 entry): froze `CHANGELOG.md` as [CHANGELOG_ARCHIVE.md](../../CHANGELOG_ARCHIVE.md) with a short pointer note at the top; created `documentation/changelog/` (README + TODO); added `tools/create_changelog_entry/create_changelog_entry.sh` (mirrors `create_bug_report.sh`, timestamped `<UTC-ISO-8601-no-colons>-<slug>.md` filenames, own test script); added a `changelog/` row to `documentation/README.md`'s folder-index; pointed CLAUDE.md's changelog rule at the new convention.

Also fixed a real functional dependency the rename would otherwise have broken silently: `tools/redundancy_scan/run.py` hardcoded `EXCLUDED = frozenset({"CHANGELOG.md"})` to keep the scan from flagging expected template-boilerplate repetition — updated to exclude `CHANGELOG_ARCHIVE.md` plus everything under `documentation/changelog/`, and `REDUNDANCY_SCAN.md`'s prose to match. Then walked every remaining plain-text `CHANGELOG.md` mention across the repo (gui/TODO.md, photo-server's README/TODO, picture-handling/TODO.md, DOC_METRICS.md, CLEANING.md, three bug files) and repointed the ones citing specific dated entries to `CHANGELOG_ARCHIVE.md`, generalized the ones making a generic claim about "the changelog." Caught and trimmed one self-introduced redundancy in the process (Joakim asked directly, mid-session, whether the new content duplicated anything): the frozen note I'd added to `CHANGELOG_ARCHIVE.md` had restated `documentation/changelog/README.md`'s full merge-hostility rationale instead of just pointing to it — cut down to a pure pointer. Removed the tooling/TODO.md bullet describing the redesign, now superseded by the built `changelog/README.md`. `app/tests` (58), `tools/redundancy_scan`+`tools/documentation_checks`+`tools/doc_metrics`+`tools/commit_cost` tests (48), and the new `create_changelog_entry.sh` test all green; dead-link sweep clean.

- **Doc size**: `CLAUDE.md` 17,145 → 17,086 (−59). `CHANGELOG.md` → `CHANGELOG_ARCHIVE.md` 162,580 → 162,918 (+338, frozen note added). `documentation/changelog/README.md` new, 2,384. `documentation/changelog/TODO.md` new, 271. `tools/create_changelog_entry/README.md` new, 82. `documentation/README.md` 1,078 → 1,225 (+147). `documentation/tooling/TODO.md` 5,082 → 2,397 (−2,685, redesign bullet removed). `documentation/tooling/REDUNDANCY_SCAN.md` 3,155 → 3,320 (+165). `documentation/tooling/DOC_METRICS.md` 6,523 → 6,521 (−2). `documentation/tooling/CLEANING.md` 7,167 → 7,178 (+11). `documentation/tooling/README.md` 4,539 → 4,539 (net 0, one label swap). Ten more files got a mechanical `CHANGELOG.md`→`CHANGELOG_ARCHIVE.md` reference fix, +8 to +56 chars each (gui/TODO.md, photo-server README+TODO, picture-handling/TODO.md, three bug files).
