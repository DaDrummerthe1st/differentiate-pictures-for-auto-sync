# Reaffirm VPS-only scope for current design work in VISION.md

Joakim flagged that nothing in current design work (tags/, upload-and-share/,
not just photo-server specifically) should assume or route through Pillar 1's
future DFS — that pillar is real but not part of the current design phase.
VISION.md's "Current build vs. this vision" section already said this for
photo-server specifically; generalized it explicitly to every current design
thread, dated, so it doesn't read as scoped to one build only.

- **Doc size**: VISION.md 6592 → 7388 chars (+796).
