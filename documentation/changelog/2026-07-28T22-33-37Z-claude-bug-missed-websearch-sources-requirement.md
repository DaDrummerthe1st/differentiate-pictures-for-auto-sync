# Claude-bug: missed WebSearch Sources requirement

Filed and immediately closed a claude-bugs entry: three `WebSearch` calls during tag/sharing design skipped the tool's own mandatory "Sources:" output requirement until Joakim asked directly for the sources used. Logged so a future session double-checks tool-mandated output requirements, not just this repo's own rules, before replying with search results folded into a synthesized answer.

- **Doc size** (Unicode codepoints): `bugs/claude-bugs/fixed/2026-07-29-missed-mandatory-sources-section-after-websearch-results.md` 0 → 1719 (new).
