# Consolidate missed bug-recurrences: 4th skipped-app-tests file, and my own AskUserQuestion lapse

Found during this cleaning pass: the 2026-07-29 consolidation of recurring "skipped app/tests" reports missed a 4th occurrence (`2026-07-27-skipped-app-tests-rerun-before-the-second-commit-this-session.md`), which chronologically sits between the current Recurrence #1 and #2 — folded in as the true Recurrence #2, renumbering the rest. Separately, this same session created a new standalone file for a decision-asked-as-prose lapse instead of reopening the near-identical existing `2026-07-19-asked-inline-instead-of-using-askuserquestion-for-a-real-user-decision.md` — folded that in too as Recurrence #2, reopened to `under_process/` since the lapse recurred. Also moved `2026-07-23-real-account-password-...md` to `fixed/` (fully resolved, no open item, was still sitting in `under_process/`) and fixed a stale path reference in POLICY.md.

- **Doc size**: this chunk is a correctness/traceability fix, not a compaction — consolidating a recurrence into its existing file necessarily adds a new Recurrence section rather than removing text. Combined across all 4 files touched (2 standalone files deleted, 2 target files grown): 12,655 → 12,933 chars (+278). The point is one findable history per recurring lapse, not fewer characters.
