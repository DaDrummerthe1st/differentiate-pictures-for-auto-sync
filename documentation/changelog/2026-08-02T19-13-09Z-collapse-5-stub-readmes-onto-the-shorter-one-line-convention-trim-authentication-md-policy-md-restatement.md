# Collapse 5 stub READMEs onto the shorter one-line convention, trim AUTHENTICATION.md POLICY.md restatement

`tools/create_bug_report/README.md` and `tools/create_changelog_entry/README.md` already used a bare "See [X](path)." stub; the other 4 code-dir stubs plus `prototypes/differentiate_pictures/README.md` carried a fuller H1-plus-sentence version conveying the same thing. Collapsed onto the shorter, already-in-use convention. Also trimmed `AUTHENTICATION.md` quoting POLICY.md's closed-by-default text verbatim instead of just linking to it.

- **Doc size**: 6 files combined 9,248 → 8,419 chars (−829).
