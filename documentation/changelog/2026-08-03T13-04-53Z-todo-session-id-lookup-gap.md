# Track the session_id lookup gap as a TODO, not just a changelog note

Joakim asked for this session's forward-effectiveness note (no documented way to reliably get the current session_id for research_log.jsonl entries) to be a tracked TODO, not just a sentence in a changelog entry. Added to documentation/tooling/TODO.md. The actual fix belongs in the global research_log.md convention (a different repo), so this entry just notes and links it rather than resolving it here.

- **Doc size**: +~830 chars (net).
