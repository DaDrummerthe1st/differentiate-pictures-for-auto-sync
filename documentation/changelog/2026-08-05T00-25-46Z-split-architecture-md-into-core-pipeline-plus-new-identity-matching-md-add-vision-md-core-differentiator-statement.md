# Split ARCHITECTURE.md into core pipeline plus new IDENTITY_MATCHING.md, add VISION.md core-differentiator statement

Joakim confirmed (via AskUserQuestion) splitting curation/ARCHITECTURE.md, which had grown to ~30K characters, as the highest remaining token-cost win for future sessions. Moved everything dated 2026-08-03 through 2026-08-05 (usage-intent scoring and its re-weighting, the privacy-decision preference aggregate, the per-household few-shot identity classifier, cross-household reuse and identity linking, the mislabeling risk flag) into a new file, `curation/IDENTITY_MATCHING.md` — ARCHITECTURE.md drops from ~30K to ~16.5K characters, keeping the three-layer pipeline, the Curator design, resource scheduling, and rollout phases. Fixed every cross-reference elsewhere in the repo (RESEARCH_QUEUE.md, GLOSSARY.md, tags/TAXONOMY.md, tags/UX_FLOWS.md, tags/TODO.md, security/THREATS.md, curation/DETECTORS.md, photo-server/DEFERRED.md) that pointed at the moved content, verified against a repo-wide grep before and after, not assumed complete.

Also added a "Core differentiator" section to VISION.md: this project isn't differentiated by picture-handling features (every photo app does that) but by giving the user complete, granular control over what's shared and with whom, and eventually what metadata reaches a central point at all. Preserved Joakim's own framing verbatim: "your privacy depends on the strength of your password," not "your privacy is what we'll tell it to be" — the thesis behind several already-decided mechanisms (no third-party OAuth, closed-by-default, subject-held-key storage) rather than a new decision.

- **Doc size**: -11,845 chars net across existing files (ARCHITECTURE.md alone: -13,767) plus +14,869 chars in the new IDENTITY_MATCHING.md — +3,024 total, but the point was per-file read cost, not total content (Unicode codepoints, per DOC_METRICS.md methodology).
