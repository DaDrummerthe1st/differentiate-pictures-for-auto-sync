# Retire all fully-merged branches, repoint docs to master-only

Audited every non-`master` branch (local + remote): `curation`, `design/icons-and-ui-libs`, `mamma-photo-viewer`, `mamma-photo-viewer-folder-ux`, `phase-1-login`, `tags`, `upload-and-share`, plus remote-only `md5_duplicate_drop`/`photo-server-planning` — all confirmed pure ancestors of `master` via `git merge-base`, nothing left to bring over. Ran the standard merge routine on `mamma-photo-viewer`/`mamma-photo-viewer-folder-ux` as a formality (no-op, already up to date both ways), then deleted all 9 branches locally and on `origin`. `master` is now the only branch, ahead of preparing the tag-GUI foundation work this session.

- **Doc size**: `documentation/README.md` -25 chars, `documentation/gui/README.md` +164, `documentation/gui/TODO.md` +133, `documentation/photo-server/TODO.md` +280, `documentation/photo-server/README.md` +288 — repointed "code lives on branch X" / "not yet done: branch reunification" statements that were now stale to reflect the master-only state; historical narrative (dated notes about work that happened on a since-deleted branch) left untouched per this repo's rule against repointing past-tense references.
