# Trim photo-server HARDWARE.md RAM entry to current facts only

At Joakim's request, collapsed the RAM row and its three surrounding historical paragraphs (upgrade-install narrative, old-stick-diagnosis theory, gate-override history) down to two flat sentences — the upgrade/diagnosis story is resolved and no longer needed at this doc's day-to-day-reference altitude. Kept the separate restart-policy-bypasses-the-gate finding, since it's an independent operational fact rather than RAM narrative.

- **Doc size**: `documentation/photo-server/HARDWARE.md` -2507 chars.
