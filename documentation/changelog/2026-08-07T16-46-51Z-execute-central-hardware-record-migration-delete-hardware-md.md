# Execute central-hardware-record migration, delete HARDWARE.md

Executed the plan `curation/TODO.md`'s "Central hardware record" section had recorded earlier today (now removed from that file — done, not a TODO anymore). That plan's target paths were stale by the time this ran: the destination repo was `workstation` when the plan was written, renamed to `hardware` and restructured into per-device category folders (`server/192.168.1.10/`, `server/161.97.174.16/`) in the meantime by a session working there directly — adapted to the current structure rather than following the stale paths literally.

Deleted `photo-server/HARDWARE.md` entirely (not a stub) — its hardware facts (i5-650, Gigabyte P55M-UD2, 16GB RAM, ZFS `raidz1` pool, network/access facts) now live in the `hardware` repo's `server/192.168.1.10/`. Moved 3 bug files that were genuinely hardware/OS incidents (a BIOS boot-priority hang, a dock SATA cable coming loose, a switch stuck-port outage) out of this repo's own `bugs/repo/` into that repo too — they were mis-filed here, same category of mistake as the plans-folder incident recorded earlier today. Moved the two shortcomings HARDWARE.md described (DDoS/resource-exhaustion risk accepted, router admin UI has no valid TLS) into `security/THREATS.md` as rows 14-15 — shortcomings, not hardware facts. Inlined the SSH-user fact and a trimmed version of the "reboot restarts the stack regardless of the memtest gate" caveat directly into `DEPLOYMENT.md`. Repointed every other cross-reference to `photo-server/HARDWARE.md` (`photo-server/{README,DEPLOYMENT,TODO,DEFERRED}.md`, `curation/{README,ARCHITECTURE,TODO}.md`, `distributed-sync/{README,HARDWARE}.md`, `upload-and-share/UPLOAD.md`, `policies/POLICY.md`, and one already-broken pre-existing wrong-depth link in a `claude-bugs/` file) to the `hardware` repo, as plain text since it's a different repo, not a markdown link. Closed `photo-server/DEFERRED.md`'s now-resolved "`HARDWARE.md` may belong at `documentation/hardware/`" item outright, per this repo's hard-facts-only convention for that file. Left `distributed-sync/HARDWARE.md` alone — that's a speculative future-product hardware doc (a NAS/router device to eventually sell), not an inventory of hardware Joakim actually owns, so it doesn't belong in the `hardware` repo.

Verified via `tools/documentation_checks/run.py`: clean except one expected broken link in an already-existing changelog entry (`2026-08-07T15-34-26Z-...md`, written before the `workstation`→`hardware` rename — left as historically accurate, per this repo's own rename/repoint convention). `tools/redundancy_scan/run.py`: no new duplication introduced.

- **Doc size**: `documentation/photo-server/HARDWARE.md` deleted (was ~4700 chars after the earlier 2026-08-07 trim). `documentation/security/THREATS.md` +1450 chars (rows 14-15). `documentation/curation/TODO.md` -2400 chars (removed the now-executed "Central hardware record" section).
