# Update dependencies to latest versions across all manifests

Joakim asked to update everything after seeing CVE warnings from GitHub; checked `gh api .../dependabot/alerts` first and found all 50 prior alerts already in `fixed` state (resolved 2026-08-07, commits `82c9f96`/`077fe73`) — no open alerts, but proceeded with a general freshness pass per Joakim's explicit ask and WORKFLOW.md's dependency-freshness rule. Bumped `fastapi` 0.116.1→0.141.1 and `uvicorn` 0.35.0→0.52.1 in `requirements.txt`/`detector/requirements.txt`; `numpy` 2.5.1→2.5.2 in `detector/requirements.txt`/`prototypes/differentiate_pictures/requirements.txt`; `mysql-connector-python` 9.7.0→26.7.0 in the untested `prototypes/` manifest (large jump, no test coverage to verify against — flagged to Joakim). Raised `server/pyproject.toml`'s lower bounds to match what `uv.lock` already had resolved to (`fastapi`, `psycopg`, `uvicorn`, `argon2-cffi`, `redis`, `slowapi`, `httpx2`) and ran `uv lock --upgrade`. `pillow`, `python-multipart`, `pyjwt`, `opencv-python(-headless)`, `onnxruntime`, `exif`, `python-magic` were already at latest. Checked all `.yaml`/`.yml` files for stale version references in comments per Joakim's mid-session ask — none found. `app/tests` (108), `detector/tests` (18), and `server/tests` (58 + 1 docker-marked) all green against the upgraded versions, no code changes needed.

- **Doc size**: this file 0 → 1,452 characters (new).
