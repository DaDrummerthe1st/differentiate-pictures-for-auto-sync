# Note invite frontend built but uncommitted and unverified at session end

Documented that app/static/*'s invite UI is built but uncommitted/unverified - live verification needs the docker-compose dev stack + Selenium, which this session couldn't launch without asking first (see the same-day claude-bug), so it's deferred to next session.

- **Doc size**: AUTHENTICATION.md +1147 chars.
