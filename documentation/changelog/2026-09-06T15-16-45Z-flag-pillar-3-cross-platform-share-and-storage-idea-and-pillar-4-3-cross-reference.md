# Flag Pillar 3 cross-platform share-and-storage idea and Pillar 4/3 cross-reference

Finishing the pillar-by-pillar native-app re-examination. Pillar 3 (presentation/sharing) turned
out bigger than the existing event/QR-code mechanism: Joakim wants the native app to be the
go-to cross-platform (Android/iOS/NAS) share solution, with a new mechanic where accepting a
share also means contributing storage for it — folding sharing into Pillar 1's DFS redundancy —
plus a paid cloud-storage tier sourced from pooled NAS/phone capacity. Flagged, not designed, per
Joakim's own call to revisit once the native app's system-design boundaries are clearer. Pillar 4
(multi-angle reconstruction) stays fully undefined — walked through its one existing paragraph in
chat since there was nothing else in the docs to react to — with one cross-reference added: it
likely builds on Pillar 3's event grouping rather than its own discovery mechanism.

- **Doc size** (Unicode codepoints): `documentation/VISION.md` 16,575 → 18,001 (+1,426).
