# Log swipe-triage and NAS-sync backlog, add contact-sheet glossary entry

Joakim sent a large feature wishlist for the Kotlin app (swipe-to-triage gesture with a hard
always-recoverable rule, bounding boxes in the grid, sort control, storage-transparency dashboard,
NAS confidence-score sync) while another AI session was concurrently active on the same branch/
checkout — logged as an unbuilt backlog in `mobile/TODO.md` instead of coding blind into a real
file-collision risk. Also answered a naming question (contact sheet / grid view / masonry layout)
into `GLOSSARY.md` per this repo's explain-in-chat-and-glossary rule.

- **Doc size**: `mobile/TODO.md` +2727 chars, `GLOSSARY.md` +738 chars.
