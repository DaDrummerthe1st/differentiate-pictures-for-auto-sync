# Add NAS data model and PWA UX flow sketch, resolve peer-session coordination

Continued the nas/ design pass: a concrete Postgres schema new to the NAS (`photos`, `devices`,
`sync_queue`, `detector_runs`, `embeddings`, reusing tags/SCHEMA.md unchanged), a Detector porting
section in ARCHITECTURE.md, and a vision-level UX_FLOWS.md sketch of the PWA's own screens. Checked
for a peer session to hand SYNC_CONTRACT.md to directly — Joakim opted to relay it himself instead.

- **Doc size**: +6030 `documentation/nas/DATA_MODEL.md`, +3608 `documentation/nas/UX_FLOWS.md`, +419 `documentation/nas/README.md`, +904 `documentation/nas/ARCHITECTURE.md`, +448 `documentation/nas/TODO.md`. Total +11409 chars.
