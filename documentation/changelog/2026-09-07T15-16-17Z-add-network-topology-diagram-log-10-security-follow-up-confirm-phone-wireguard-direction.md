# Add network topology diagram, log .10 security follow-up, confirm phone WireGuard direction

Added NETWORK.md's mermaid diagram (home-LAN vs. away/WireGuard-via-two-ISPs paths), confirmed
the phone reaches the NAS the same WireGuard way a remote PWA browser would (no new mechanism),
and logged a `.10` security follow-up section in TODO.md (auth crash-loop diagnostic command,
login/access audit, the "buzzkit" question, old-stack retirement) surfaced by Joakim's own
inventory pass — none of it run or resolved by this session, handed over as copyable commands.

- **Doc size**: +2817 `documentation/nas/NETWORK.md`, +470 `documentation/nas/README.md`, +1900 `documentation/nas/TODO.md`. Total +5187 chars.
