# changelog/ — open work

- **Browsable/rendered view of all entries** — deferred at build time (2026-07-23); `ls`-and-read works fine per `bugs/`'s own precedent, revisit only if the file count makes that genuinely painful. Not started.

Nothing else planned right now.
