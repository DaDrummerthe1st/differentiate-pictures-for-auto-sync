# TODO

- Implement `exposure_percent` and `saturation_percent` in `modules/blur_check.py` (or a renamed successor covering all three), per [QUALITY_METRICS.md](QUALITY_METRICS.md)'s design.
- No real `file_id`/quality table exists yet — this stays design-only until the standalone script (or its output) is wired into an actual database.
